
// Page navigation is now integrated into the main Navigation component
// This component is no longer needed but kept for compatibility

const PageNavigation = () => {
  return null;
};

export default PageNavigation;
